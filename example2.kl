define _start() {
    int num = int(input("Enter a number: "))
    if num > 0 then {
        println(num + " is positive")
    }
    elseif num < 0 then {
        println(num + " is negative")
    }
    else {
        println("You entered 0")
    }
}