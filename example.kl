define _start() {
    </
    I know it's a wonky example
    />

    bool cool = Yes
    bool hungry = No

    if cool and !hungry then {
        println("Valid")
    }
    if !cool or !hungry then {
        println("Also Valid")
    }

    if cool || hungry then {
        println("Absolutely Valid")
    }

    if not hungry then {
        println("New features work!")
    }
}