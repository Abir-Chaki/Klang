define _start() {

    bool isCool = Yes       ?? Tokenized to BOOL1
    bool hungry = No        ?? Tokenized to BOOL0

    if isCool then {
        println("You are Cool")
    }
    if hungry == Yes then {
        println("You are hungry")
    }
}